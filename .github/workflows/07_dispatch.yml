name: Workflow Manual
on:
  workflow_dispatch:
    inputs:
      nombre:
        description: 'Raul Sánchez Serrano'
        required: true
        default: 'Raúl'
jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo personalizado
        run: echo "Hola, ${{ github.event.inputs.nombre }}!"